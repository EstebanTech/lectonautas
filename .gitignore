# Variables de entorno reales (la plantilla versionada es .env.example)
.env
*.env
!.env.example

# Binarios y logs
bin/
*.log

# Configuración local de Claude Code: sesiones y ajustes de esta máquina.
# CLAUDE.md NO va aquí: son las convenciones del repo y se versionan, igual que
# la colección de Postman, que es la documentación ejecutable de la API.
.claude
